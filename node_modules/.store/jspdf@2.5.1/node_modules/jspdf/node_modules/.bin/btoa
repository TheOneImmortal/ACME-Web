#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/D:/Xn/Code/Codes/vue/Acme/Acme网页/acme管理系统/node_modules/.store/btoa@1.2.1/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/D:/Xn/Code/Codes/vue/Acme/Acme网页/acme管理系统/node_modules/.store/btoa@1.2.1/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../btoa@1.2.1/node_modules/btoa/bin/btoa.js" "$@"
else
  exec node  "$basedir/../../../../../btoa@1.2.1/node_modules/btoa/bin/btoa.js" "$@"
fi
